
type UnsignedByte : Integer(min=0, max=255)

object Color {
	red: UnsignedByte,
	green: UnsignedByte,
	blue: UnsignedByte,
	alpha: UnsignedByte
}

object TranslucentColor extends Color {
	alpha: UnsignedByte
}

root {
	some_color: Color,
}
