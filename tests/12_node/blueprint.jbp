
enum origin {
	ACQUIRED,
	RECEIVED,
	GUESSED
}

node position {
	latitude: float (min=-90, max=+90),
	longitude: float (min=-180, max=+180),
	origin: origin
}

root {
	position: position,
	moment: datetime
}
